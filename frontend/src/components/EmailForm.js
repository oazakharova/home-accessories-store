import React from 'react';

import styles from './EmailForm.module.css';

const EmailForm = () => {
  return <div className={`${styles.emailFormWrap} container`}>EmailForm</div>;
};

export default EmailForm;
