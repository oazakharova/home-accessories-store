import React from 'react';

import styles from './NewArrivals.module.css';

const NewArrivals = () => {
  return (
    <div className={`${styles.newArrivalsWrap} container`}>NewArrivals</div>
  );
};

export default NewArrivals;
